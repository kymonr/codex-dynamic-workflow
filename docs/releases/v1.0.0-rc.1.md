# v1.0.0-rc.1 发布候选说明

## 状态

```text
candidate_version: v1.0.0-rc.1
source_master: 8177db80f7b60f376fde73e71bc9b19af8fa9b48
source_tree: ae70b0c94b42cf165da398c463a514bdb2c5e6c5
release_tag_created: false
github_prerelease_created: false
published_skill_updated: false
release_or_deploy_authorized: false
```

本文件定义发布候选，而不是宣告已经发布。候选必须经过本地迁移、隔离安装和真实业务仓库试运行；只有全部门禁闭合后，才允许创建 `v1.0.0-rc.1` tag/GitHub prerelease。稳定版 `v1.0.0` 另需观察期后的独立决定。

## 候选能力

- Workflow IR v3 与可信 `agent / map / verify / reduce / conditional / human_gate / bounded loop` runtime。
- checkpoint、events、CAS artifact、显式 gate decision、resume/no-replay 和 whole-workflow deadline。
- `design-swarm`、`ultra-review`、`repo-sweep` 固定预设。
- Auto Planner v1 固定注册表选择。
- Worktree Writer v1 隔离候选流程：package → detached worktree → Luna Writer → effect reconciliation → fixed validation → frozen candidate → fresh Sol review → status/export。
- Windows 与 Ubuntu CI，以及多个真实 Windows RC 证据。

## 不包含的能力

- 模型生成任意可执行 DAG。
- 自动把 Writer candidate 应用到 canonical checkout。
- 自动 `git add`、commit、push、merge、release 或 deploy。
- Writer 隐藏 retry、模型升级、nested agent 或 reviewer 驱动的自动修复循环。
- 对 `observed_sandbox` 的独立宿主 attestation；该字段仍可能为 `unknown`。

## 发布门禁

### G1 — master 保护

- [ ] `master` 启用 branch protection。
- [ ] 必须经 Pull Request。
- [ ] 必需 status checks：`Python 3.12 / ubuntu-latest`、`Python 3.12 / windows-latest`。
- [ ] 要求分支保持最新后再合并。
- [ ] 要求 conversation resolution。
- [ ] 禁止 force push。
- [ ] 禁止 branch deletion。
- [ ] 管理员是否受规则约束必须明确记录。

### G2 — 旧本地工作区审计

目标：`D:\codex\projects\codex-dynamic-workflow`

- [ ] 只读记录 branch、HEAD、upstream、remote、status、staged/unstaged/untracked 清单。
- [ ] 导出 binary-safe patch、untracked manifest 和逐文件 SHA-256 到仓库外证据目录。
- [ ] 扫描凭据、Cookie、token、客户数据、浏览器 profile、数据库和绝对路径泄漏。
- [ ] 不执行 `reset --hard`、`clean`、`stash`、`pull` 或覆盖式 checkout。
- [ ] 将每项本地改动分类为：保留为补丁、已被上游取代、独立后续分支、可明确丢弃。

### G3 — 干净 checkout

- [ ] 从 canonical `https://github.com/kymonr/codex-dynamic-workflow.git` 创建新目录。
- [ ] detached 或 `master` 精确跟踪 `8177db80…`，随后确认与远端当前 master 一致。
- [ ] 工作树 clean，无 `.pyc`、`__pycache__`、reparse、nested repository 或 alternates。
- [ ] compileall、policy consistency、offline routing smoke、完整 unittest、`git diff --check` 通过。

### G4 — 隔离安装

- [ ] 使用全新 `CODEX_HOME`，不覆盖日常安装。
- [ ] 复制 `skill/` 到 `$CODEX_HOME/skills/dynamic-workflow/`。
- [ ] 只复制 `config/agents/` 中启用的 `.toml`。
- [ ] 不启用 `grok_writer.toml.disabled`。
- [ ] 合并 workspace AGENTS 规则，不覆盖原有项目规则。
- [ ] 新任务重新加载 Skill。
- [ ] 安装树与候选 source 逐文件 hash 对齐。

### G5 — 订单采集器只读试运行

目标仓库：`kymonr/shopee-order-collector`

当前待重新核对的候选身份：

```text
branch: codex/zclaw-cli-r9
head: 577375d3cc013442c4df920dc0e31afa6d78d1a3
pr: #21 / Draft
```

- [ ] 重新核对远端和本机精确 Head；不得把以上快照直接当作当前事实。
- [ ] 使用 sanitized tracked-only clone；不发送真实 Cookie、代理认证、店铺账号、订单正文、客户 PII、WPS 文件或浏览器配置。
- [ ] 先 `auto-plan` 或直接选 `ultra-review`；需要宽范围模块清单时再使用 `repo-sweep`。
- [ ] 所有 agent 保持 read-only；到 human gate 停止。
- [ ] 审阅真实 prompt、map/verify linkage、verdict、artifact、events、target fingerprint 和 no-replay。
- [ ] 不执行 gate decision、resume 或 Writer。

### G6 — 最小 Writer candidate

- [ ] 只从 G5 已验证的问题中选择一个低风险、1–2 个 owned targets 的 create/modify package。
- [ ] package 使用精确 Head/tree、closed Schema、canonical digest、fixed argv verification。
- [ ] `writer-plan` 必须零模型、零写入。
- [ ] `writer-run` 只执行一次；一个 Luna Writer、一个 fresh read-only Sol reviewer。
- [ ] 无 retry、upgrade、nested agent、network、Git mutation 或 canonical apply。
- [ ] 两次 `writer-status` 与一次 `writer-export` fingerprint 一致。
- [ ] 默认保留 worktree、lock 和 evidence，不 cleanup。

### G7 — 人工应用门

- [ ] 用户查看 candidate patch、文件内容、验证结果、reviewer findings 和 limitations。
- [ ] 用户明确选择 `apply`、`fix-first`、`rethink` 或 `discard`。
- [ ] 未收到明确 apply 决定前，不修改订单采集器 canonical checkout。
- [ ] apply 采用独立、可审计的人工流程；不由 Writer runtime 自动执行。

### G8 — RC tag 与 prerelease

只有 G1–G7 全部完成后：

- [ ] release branch/PR 的 Windows、Ubuntu CI 全绿。
- [ ] 合并 CHANGELOG/发布说明到受保护 master。
- [ ] tag 精确绑定通过门禁的 master commit。
- [ ] 创建 GitHub prerelease `v1.0.0-rc.1`，附 source commit/tree、CI 和试运行摘要。
- [ ] 不自动更新日常 Skill；隔离安装继续保留。

### G9 — 稳定版观察

- [ ] 记录 RC 期间运行次数、workflow/preset、terminal state、agent claims、失败/阻断、retry/upgrade、artifact 完整性和人工 gate 决定。
- [ ] 没有未解释的 target mutation、重复执行、权限扩张、checkpoint/digest mismatch 或 Writer 越权。
- [ ] 重要问题先进入新的修复 PR 和 RC，不直接重写已发布 tag。
- [ ] 观察期通过后，单独决定 `v1.0.0`；不因 RC 发布自动升级为稳定版。

## 建议验证命令

```powershell
$env:PYTHONDONTWRITEBYTECODE = '1'
$env:PYTHONPYCACHEPREFIX = Join-Path $env:TEMP `
  ('dynwf-rc1-' + [guid]::NewGuid().ToString('N'))

python -m compileall -q skill
python skill\scripts\check_policy_consistency.py --json
python skill\scripts\routing_smoke.py
python -m unittest discover -s skill\tests -v
git diff --check
```

`routing_smoke` 只能作为离线 evaluator 证据，不能代替真实 agent route 或 sandbox 证据。

## RC 通过条件

```text
master protected
old local changes preserved and classified
clean checkout verified
isolated install hash-matched
order collector read-only audit completed
one minimal writer candidate reviewed manually
no unauthorized effects
release branch CI green
explicit human authorization for tag/prerelease
```

在上述条件未全部成立前，候选状态保持：

```text
RC_PREPARED_NOT_PUBLISHED
```
