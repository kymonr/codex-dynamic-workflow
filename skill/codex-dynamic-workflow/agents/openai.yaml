interface:
  display_name: "Codex Dynamic Workflow"
  short_description: "Astra mainline and proactive Luna probes"
  default_prompt: "Use $codex-dynamic-workflow for a complete Astra mainline and at least three distinct optional Luna probes on meaningful tasks when capacity permits, within the current authorization and budget."
policy:
  allow_implicit_invocation: true
