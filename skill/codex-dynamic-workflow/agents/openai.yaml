interface:
  display_name: "Codex Dynamic Workflow"
  short_description: "Implicit Luna probes; explicit full workflow"
  default_prompt: "Use $codex-dynamic-workflow in explicit full-workflow mode: Astra designs and accepts, Sol implements and tests, and Luna adds distinct optional evidence within budget and capacity."
policy:
  allow_implicit_invocation: true
