interface:
  display_name: "Codex Dynamic Workflow"
  short_description: "Implicit Luna and Grok probes; explicit full workflow"
  default_prompt: "Use $codex-dynamic-workflow in explicit full-workflow mode: Astra plans and sets acceptance checks, a user/host switch lets Sol own the execution main thread, a fresh Astra context reviews important deliverables, and broad readonly Luna probes add evidence without a batch-completion barrier. Preserve budget, capacity and required gates."
policy:
  allow_implicit_invocation: true
