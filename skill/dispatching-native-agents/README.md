# dispatching-native-agents (deprecated)

This directory is a compatibility alias for `codex-dynamic-workflow`.

Canonical Skill:

```text
$codex-dynamic-workflow
```

The alias is explicit-only (`allow_implicit_invocation: false`) to avoid duplicate
auto-routing. It does not contain a second workflow implementation.
