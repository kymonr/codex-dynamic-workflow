# 全原生联合验收

本轮联合验收通过，允许进入本次授权的 GitHub 提交阶段。验收对象是普通 Luna 路由、质量边界与全原生限制；不是模型质量对照或原生容量压力测试。

## 候选与方法

- Git 基线：`ee0cda6d567340d25487d675ed6b3bdfcae74889`，仓库 `kymonr/codex-dynamic-workflow`。
- 原源码根：`D:\codex\projects\codex-dynamic-workflow`；交付分支：`codex/native-luna-joint-acceptance`。
- 初始候选包含 73 个绑定源文件，与基线相比有 25 个变更路径。主线程记录逐文件 SHA-256；代理直接读取原始文件，Astra 使用固定 Git commit 读取基线。
- 使用原生 Skill-only 派工：两条 Luna/max 证据支线与一条 Astra/high 关键复核支线；文案修正后追加一次原生 Luna/max 窄复核。共 4 次原生子代理执行，没有 CLI 模型调用。
- 三次连续原生状态查询分别报告三条初始支线为 running，随后记录于 `2026-09-06 11:09:37 UTC`。这是原生派工与状态观察证据，不是原子宿主快照或服务端推理同时执行的证明。
- 所有模型/effort 均为请求路由；缺少独立宿主身份回执，实际有效模型保持 `UNKNOWN`。本次使用已暴露的兼容 Luna/max profile，不宣称新 `cwf_general` 已在当前宿主热加载。

## 检查与处理

| 分支 | 原始证据与实际检查 | 结果 |
|---|---|---|
| Luna 文档与入口 | 13 个绑定文件，逐项核对当前说明、profile、示例和公开入口 | 发现普通/机械 Luna 文案歧义，以及两个旧 exec 示例缺少明确停用标记 |
| Luna 预算矩阵 | 5 个源码文件；纯内存执行 17 组有效输入、10 组无效输入 | 最终全部符合逐项预期；覆盖普通额度、机械 reserve、必要 Astra 预留、容量和 absolute 边界 |
| Astra 关键复核 | 16 个绑定文件；原始基线/候选对比、纯内存 Runtime 与故障注入探针 | 未发现本次范围内需阻断提交的具体缺陷；4,320 组输入确认旧预算默认语义一致 |
| Luna 修正复核 | 3 个后继候选文件与入口守卫对照；JSON 内存解析 | 两类文档问题均已处理，3 个文件与后继绑定一致 |

主线程采纳原始证据并修正三处文案：Skill README 明确区分 `cwf_general` 的普通 Luna/max 与 `cwf_mechanical` 的机械 Luna/medium；两个 exec 示例的 `goal` 标记为 `RETIRED compatibility fixture`，并明确当前原生入口会拒绝它们。Runtime 代码未在复核后改变。

预算矩阵第一次补充探针把一个 reserve 用尽案例同时设在 absolute 上限，得到正确的 absolute-stop，与当时错误的预期不符；修正输入后重新计算通过。这是探针设置错误，未当作产品缺陷或隐去该次结果。

## 主线程验证

- 交付 Git 副本完整回归：254 项执行，253 通过，1 项因 Windows 符号链接权限跳过；70.895 秒。
- 三处文案修正后：98 项合同检查全部通过；44.094 秒。包验证器 PASS。
- 73 个交付文件与对应源码文本一致；Git 按仓库既有属性保存 LF，原源码按各自字节哈希绑定，不混淆文本相同与原始字节相同。
- 修正后安装更新 1 个说明文件，25 个受管文件均与源码字节一致；预检 `changes=[]`，旧别名未重新启用。
- 最新本地安装收据：`reports/install-backup-4194125dd8dd/receipt.json`。收据和前像留在本地，不加入 Git 提交。
- 所有代理均已返回并声明其工具调用结清。原生 turn 完成不等于已证明宿主会话关闭或槽位释放。

原始冻结清单、后继清单、状态观察、安装读回及测试日志位于本任务专用目录 `D:\codex\temp\dynamic-workflow-native-joint-20260906`。公开提交清单由 `SOURCE_MANIFEST.json` 记录。

## 保留的边界

当前原生工具是全部模型派工入口；普通与关键工作共享实际宿主容量，满载时排队。公开 `exec-one`、`luna-pool` 与新 exec 准入已经停用。低层 exec 适配器及旧记录保留用于兼容和确定性验证；这不是操作系统层面禁止任意 Python 导入或外部 CLI 的保证。

本次没有选择 native Runtime 持久账本的端到端验收，没有压满全部原生槽位，没有检验真实模型身份、会话释放或比较 Luna/Astra 的任务质量。GitHub 提交和 CI 结果由随后产生的实际 commit、PR 与检查回执确认，不由本报告预先宣称。
