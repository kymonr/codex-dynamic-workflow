# Ordinary Luna routing — local acceptance

User approved ordinary bounded readonly delegation through Luna/max, mechanical checks through Luna/medium, and Astra for complex judgment and critical independent acceptance. Source root: `D:\codex\projects\codex-dynamic-workflow`. This phase includes local source changes and installation; no commit, push or merge.

## Delivered behavior

- Skill-only decomposition prefers useful independent ordinary evidence work on `cwf_general` (Luna/max), with sufficient sources and checks. Mechanical work stays `cwf_mechanical` (Luna/medium). There is no target model ratio or agent quota.
- Runtime adds `ordinary` routing with `ordinary_qualified: true`. Explicit strong routing wins; unqualified work remains strong. Ordinary low/medium-risk readonly verification is permitted with target dependency, distinct host identity and full matching source coverage.
- High-risk targets and writers require strong verification regardless of the checker's risk label. Ordinary calls consume approved non-strong allowance, never the mechanical reserve. Existing three-route contracts and old node records remain usable without rewriting their routes, hashes, budgets or attempts.
- The self-contained readonly `cwf_general.toml` is a new package-owned target. Existing user `luna.toml` and global configuration are outside the installer write manifest.

## Verification

- Full source regression: `python -B -m unittest discover -s tests -q`: 231 cases, 230 passed, one Windows symlink-privilege skip, 47.690 seconds. Thirteen new routing/installation tests cover ordinary defaults/roles, explicit and legacy routes, verification identity/coverage, risky-target checks, budget isolation, profile collisions and preservation/rollback.
- Package validator: PASS. Generic Skill Creator quick validation could not run: `ModuleNotFoundError: No module named 'yaml'` with both existing Python runtimes. No dependencies were installed; this check is not reported as passed.
- Independent non-author source review compared all 12 candidate files with commit `d302ae04841793748b7f4481f70aa05b6115422d`. No newly actionable defect found within that scope. Separate pure-memory probes verified ordinary exec arguments and 8,340 valid optional-budget states. A first mock argument construction error was corrected by the reviewer; it was not a product failure or a passing check.
- A Luna/max-requested branch performed the real bounded packaging inventory. Its subsequent scenario assessment routed module/documentation comparisons and pure-function investigation to ordinary Luna, mechanical manifest checks to Luna/medium, and ownership/security-critical work to Astra. This is a Skill-only routing assessment; its conditional Runtime prose was not an executed Runtime plan or schema validation.
- Requested agent calls in this phase: two Luna/max turns and one Astra/high review. Effective model identities were not exposed by independent host receipts and remain UNKNOWN. No new real `codex exec` model smoke was launched.

## Initial installation and recovery

Observed process and user `CODEX_HOME`: `D:\CodexData\.codex`.
Installer applied eight changed files and verified all 23 managed files. Post-install dry-run returned `changes: []`; absent legacy alias stayed retired.
Direct import from the installed `skills/codex-dynamic-workflow/scripts/cwf_runtime/core.py` returned `ordinary = {model: gpt-5.6-luna, effort: max, profile: cwf_general}`. Installed and source profile hashes match:
`1d9ace6ac35c5c51011cfce055051052781430266a1734dfd74406a9922cdea5`.

Recovery receipt: `reports/install-backup-509200fad3ac/receipt.json`. Restore only through the existing scoped rollback command after checking for subsequent installation drift.
Candidate hashes and comparison diff: `D:\codex\temp\dynamic-workflow-luna-20260906\candidate-sha256.json` and `candidate.patch`. Full test output: the same task directory's `regression.log`.

Installed bytes and local behavior are verified. New-profile activation in a fresh Desktop session, hot reload, effective model identity and broad real-workload quality/cost remain unverified. Profiles and qualification flags describe requested execution and trusted planning decisions; they do not prove host sandbox enforcement or model capability.

## Approved quality-first clarification

The user subsequently approved keeping Luna on recomputable/source-checkable deliverables while retaining Root/capable judgment and any required independent review for conclusions beyond check coverage. The Skill, host-routing/runtime references, general profile, README and changelog now distinguish precise negative check results from broad defect-absence, repair-completeness and acceptance claims. Root must inspect coverage even when Luna reports no gap. Low impact, few lines and read-only access do not establish task simplicity.

This clarification changes instructions only. Runtime core and budget code retain the hashes independently reviewed above. The focused routing/install suite passed all 13 tests in 3.066 seconds; package validation passed. The earlier 231-case result is retained as the unchanged-code regression evidence, not reported as a newly rerun full suite.

A fresh non-author Astra/high-requested agent read the four executable instruction inputs and evaluated five scenarios: manifest equality, a small authorization module, pure-function boundary cases, repair completeness after old tests pass, and incomplete caller coverage. It found no rule contradiction that forces deterministic negative results onto Astra or promotes bounded PASS results into broad assurance. This was a rule scenario assessment, not comparative task-quality measurement; effective model identity remains UNKNOWN.

The four reviewed instruction files matched their review hashes before installation. Applied four changes, verified all 23 managed files, and read back an empty dry-run. Latest recovery receipt: `reports/install-backup-e057b1310875/receipt.json`; this supersedes the initial receipt for the current installed instructions. Current general profile SHA256: `5d9b6f42a1a8f4c773ef43f11d595b57c848252e08bb917fe1fc0bb6cc774c99`.

No Runtime code, model mapping, budget or Git publication changed in this clarification. Real-task false findings, omissions and rework have not been compared between the old and new routing schemes.
