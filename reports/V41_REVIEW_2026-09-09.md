# v4.1 follow-up review

Baseline: e5f3d6e9b15419724ff6eda5b46a657f61016c58 (PR #41).
Reviewer: implementing Root, a second complete diff/source pass; not a non-author model review.

Reviewed the entire change set: native route validation, saved-contract feature gating,
result schemas, early reports, screening/duplicates, objective-aware issue resolution,
acceptance keys, capacity reservations, candidate epochs, closeout observations, CLI,
package validation, instructions, comparison evaluator and positive/negative regressions.

## Findings repaired in this pass

- CLI triage initially omitted its new duplicate choice although the Python API supported it.
  Exposed the choice and added a CLI persistence regression.
- Explicit resolution could select unrelated old completed Astra evidence. It must now use
  the promoted investigation itself or a causal mainline descendant; added a rejection test.
- The general Luna profile still suggested placing additional material leads only in uncovered
  scope. It now requires every plausible material lead in CLAIMS, including early reports.
- The comparison prototype initially excluded failed-acceptance arms. It now retains failures
  and their end times, preventing successful-trial-only reporting.
- Keep legacy result.schema.json distinct from result-v41.schema.json; legacy executor tests
  and saved-contract tests remain active, rather than silently broadening old contracts.

Earlier test failures are retained in task logs: the initial notes-schema mismatch and
package-copy local-link failures were repaired without weakening the validator. No model
execution, authentication or safety gate was bypassed to obtain a passing test.

## Verification and limits

Reviewed Linux/container regression: 353 tests, 352 passed, one Windows-only skip,
23.197 seconds; package validator and diff whitespace checks passed. Windows and installation
results belong to the separate acceptance report, not this source-review claim.

No further concrete blocker was found in the examined diff and deterministic scenarios.
Semantic risk screening, honest host observations and correctness of evidence remain Root
responsibilities. Notes are not permission to hide risks. A completed investigation is not
repair acceptance. Closeout records create no worker, stop no process and release no slot.
Reopening is restricted to still-open runs and changed bound bytes; it does not resurrect
closed runs or reset any budget. Ordinary work remains Skill-only.

Native model execution, effective identity, non-author model review and paired task-quality
comparison were not performed by the deterministic test suite. See
V41_NATIVE_COMPARISON_STATUS.json for the actual external validation boundary.
